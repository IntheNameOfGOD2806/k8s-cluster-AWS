apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "isc-fe.fullname" . }}-config
  labels:
    {{- include "isc-fe.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
