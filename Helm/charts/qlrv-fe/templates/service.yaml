apiVersion: v1
kind: Service
metadata:
  name: {{ include "qlrv-fe.fullname" . }}
  labels:
    {{- include "qlrv-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8888
      protocol: TCP
      name: http
  selector:
    {{- include "qlrv-fe.selectorLabels" . | nindent 4 }}
