apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "isc-be.fullname" . }}-config
  labels:
    {{- include "isc-be.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
  
