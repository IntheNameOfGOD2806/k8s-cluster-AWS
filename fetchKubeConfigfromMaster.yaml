- hosts: all
  name: fetch kubeconfig file
  gather_facts: false
  tasks: # Bỏ become: yes
    - name: fetch kubeconfig file
      ansible.builtin.fetch:
        src: /home/ubuntu/.kube/config
        dest: ~/.kube/config # Dùng ~ thay vì hardcode
        flat: yes
        mode: "0600" # Quan trọng: kubeconfig phải restrict permission
